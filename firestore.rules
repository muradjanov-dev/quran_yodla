rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only server (admin SDK) can read/write — no client-side access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
